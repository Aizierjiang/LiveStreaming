# Settings config file format warnings:
# Only one ':' is allowed each line.
# No indents allowed among characters.
# No dots or other characters allowed at the end of each line.

IP:172.26.5.184
HTTP-PORT:8080
WS-PORT:8383